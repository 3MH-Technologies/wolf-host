# Pin the lint rule set explicitly so local runs and CI always agree,
# regardless of the default rule set of whichever ruff version is installed.
# These are ruff's classic default rules (E4/E7/E9/F).
[lint]
select = ["E4", "E7", "E9", "F"]
